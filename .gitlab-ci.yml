stages:
    - build
      
build:
    stage: build
    tags:
        - hishinuma-ci
    script:
        - make
        - ls
        - ls lib/
        - ls lib/qd
        - ls lib/qd/include
        - ls lib/qd/lib/

